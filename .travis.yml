jdk:
  - oraclejdk8
  - oraclejdk9
language: scala
sudo: false
script:
   - sbt ^test ^scripted
   - cd examples
   - for d in */ ; do cd "$d" && if [ ! -f "./project/build.properties" ]; then echo "sbt.version=0.13.17" > project/build.properties ; fi && sbt test && cd ../ ; done

